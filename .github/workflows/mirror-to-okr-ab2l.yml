name: Mirror docokr to okr-ab2l

on:
  push:
    branches: [main]

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push to okr-ab2l repo
        run: |
          git config user.name "docokr-mirror-bot"
          git config user.email "bot@users.noreply.github.com"
          git remote add lovable git remote add lovable https://x-access-token:${{ secrets.MIRROR_TOKEN }}@github.com/docdanielmarques/okr-ab2l.git
          git push --force lovable main:main
